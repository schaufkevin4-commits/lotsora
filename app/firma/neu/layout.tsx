import { Arbeitsbereich } from "@/components/layout/arbeitsbereich";

export default function FirmaLayout({ children }: { children: React.ReactNode }) {
  return <Arbeitsbereich>{children}</Arbeitsbereich>;
}
